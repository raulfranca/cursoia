# Checkbox

Square ink checkbox with an inline label.

```jsx
<Checkbox checked={ok} onChange={setOk}>Quero receber as aulas por e-mail</Checkbox>
```

Controlled. The box is a 1.5px ink square that fills solid ink when checked — no green, no blue.
