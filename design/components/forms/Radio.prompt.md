# Radio

One option in a single-choice group.

```jsx
<Radio name="etapa" value="fund1" checked={v==="fund1"} onChange={setV}>Fundamental I</Radio>
```

Always render 2+ together with a shared `name`, stacked with `--space-4` between.
