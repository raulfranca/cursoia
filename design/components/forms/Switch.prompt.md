# Switch

Binary toggle for immediate settings.

```jsx
<Switch checked={on} onChange={setOn}>Receber lembretes</Switch>
```

On state fills chalkboard green. Use for settings that apply instantly; use Checkbox inside forms that are submitted.
