# Select

Native select with the brand chevron.

```jsx
<Select label="Etapa em que você atua" placeholder="Escolha uma opção" options={["Educação Infantil","Fundamental I","Fundamental II","Ensino Médio"]} />
```

`options` accepts plain strings or `{value,label}`. Keep lists under ~8 items; beyond that use Radio groups or search.
