# Stat

One large figure with a caption.

```jsx
<Stat value="4 semanas" label="Duração total" variant="rule" />
```

Group 3–4 in a flex row with `--space-9` gaps; use `variant="rule"` for all but the first when you want hairline dividers.
