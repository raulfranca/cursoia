# Tooltip

Hover label wrapping its trigger.

```jsx
<Tooltip label="Salvar para depois"><IconButton icon="bookmark" label="Salvar" /></Tooltip>
```

`open` forces visibility for specimens. Never put essential information only in a tooltip.
